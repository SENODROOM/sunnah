# Changelog

## [1.1.0] — 2026-03-21

### Added
- Python package published to PyPI (`pip install sunan-ibn-majah`)
- Python API identical to npm — same camelCase method names
- Monorepo structure — single `data/majah.json.gz` shared by JS and Python
- `data/`, `src/`, `types/`, `scripts/`, `python/`, `examples/`, `docs/`, `tests/`
- `.github/workflows/` — auto-publish to npm and PyPI on GitHub release
- Offline-first — data bundled, no CDN required

## [1.0.0] — 2026-01-01
### Added
- Initial release with 4,341 hadiths
